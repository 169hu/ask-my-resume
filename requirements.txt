# 核心 UI
streamlit>=1.36,<2

# 向量库 & 向量化
chromadb>=0.5.0,<1
sentence-transformers>=3.0.0,<4

# urllib3 / requests（Python 自带 urllib.request，这里补 requests 以防 sentence-transformers 依赖版本漂移）
# （留空即可，sentence-transformers 会自行带上）
